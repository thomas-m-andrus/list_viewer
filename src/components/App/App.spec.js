import { shallowMount, mount} from '@vue/test-utils'
import AppComponent from './index.vue';


// Here are some Jasmine 2.0 tests, though you can
// use any test runner / assertion library combo you prefer
describe('AppComponent', () => {
  it("AppComponent houses the router view",()=>{
    
  })
  it("backToHome - will take the user back to the home page regaurdless of current location", () =>{

  })
  it("has a title element in the html that is connected to the backToHome function which will trigger when clicked", () =>{

  })
})